<h1>Your picks</h1>
<%= @ordered_items.each do |item| %>
  <%= item.item.name %>
  <%= item.item.description %>
  <%= item.item.price %>
  <% @totalprice += item.item.price %>
<% end %>
<%= @totalprice %>

